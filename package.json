{
  "name": "last-epoch-deck-companion",
  "version": "0.1.8",
  "private": true,
  "type": "module",
  "description": "Local LAN companion for sending Last Epoch Steam Deck snapshots to a laptop analyzer.",
  "scripts": {
    "start": "node server/src/server.js",
    "dev": "node server/src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
